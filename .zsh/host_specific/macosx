if [ -r /mach_kernel ]; then
    alias v='open'
    export MANPATH="/opt/local/man:$MANPATH"
    export PATH="/opt/local/bin:/opt/local/sbin:$PATH"
    alias upgrade='dports_upgrade'
    # always have DISPLAY set
    if [ ! $DISPLAY ]; then
	export DISPLAY=:0.0
    fi
fi
